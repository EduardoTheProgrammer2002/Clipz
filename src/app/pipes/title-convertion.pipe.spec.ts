import { TitleConvertionPipe } from './title-convertion.pipe';

describe('TitleConvertionPipe', () => {
  it('create an instance', () => {
    const pipe = new TitleConvertionPipe();
    expect(pipe).toBeTruthy();
  });
});
